<p id="notice"><%= notice %></p>

<h1>Listing Hotspots</h1>

<table>
  <thead>
    <tr>
      <th>Location</th>
      <th>Films</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @hotspots.each do |hotspot| %>
      <% if !hotspot.nil? %>
        <tr>
          <td><%= hotspot.location %></td>
          <td><%= hotspot.destination %></td>
          <td><%= link_to 'Show', [@film, hotspot] %></td>
          <td><%= link_to 'Edit', edit_film_hotspot_path(@film, hotspot) %></td>
          <td><%= link_to 'Destroy', [@film, hotspot], method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    <% end %>
  </tbody>
</table>
