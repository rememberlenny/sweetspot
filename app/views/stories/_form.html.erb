<%= form_for(@story) do |f| %>
  <% if @story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@story.errors.count, "error") %> prohibited this story from being saved:</h2>

      <ul>
      <% @story.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :byline %><br>
    <%= f.text_area :byline %>
  </div>
  <div class="field">
    <%= f.label :blurb %><br>
    <%= f.text_area :blurb %>
  </div>
  <div class="field">
    <%= f.label :first_slide %><br>
    <%= f.text_area :first_slide %>
  </div>
  <div class="field">
    <%= f.label :featured_photo %><br>
    <%= f.text_area :featured_photo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
