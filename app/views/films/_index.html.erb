<p id="notice"><%= notice %></p>

<table class="table">
  <thead>
    <tr>
      <th>ID </th>
      <th>Image </th>
      <th>Title</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @films.each do |film| %>
      <tr>
        <th><%= link_to film.id, [@story, film] %> </th>
        <td><%= link_to image_tag(attachment_url(film, :image, :fill, 250, 150)), [@story, film] %></td>
        <td><%= link_to film.title, [@story, film] %></td>
        <td><%= link_to 'Edit', edit_story_film_path(@story.id, film) %> | <%= link_to 'Destroy', [@story, film], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Film', new_story_film_path(@story.id) %>
