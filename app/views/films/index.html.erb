<p id="notice"><%= notice %></p>

<h1>Listing Films</h1>

<table class="table">
  <thead>
    <tr>
      <th>Image </th>
      <th>Title</th>
      <th>ID </th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @films.each do |film| %>
      <tr>
        <td><%= link_to image_tag(attachment_url(film, :image, :fill, 250, 150)), film %></td>
        <td><%= link_to film.title, [@story, film] %></td>
        <td><%= link_to film.id, [@story, film] %></td>
        <td><%= link_to 'Edit', edit_story_film_path(film) %> | <%= link_to 'Destroy', [@story, film], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Film', new_film_path %>
